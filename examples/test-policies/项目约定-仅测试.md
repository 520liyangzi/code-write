---
example_only: true
status: test_only
scope: project
---

# 模拟项目约定（仅测试）

> 本文档为虚构测试数据，不代表任何实际项目要求，禁止用于正式激活。

## TEST-PROJECT-001 配套文件

新增 `*Gateway.java` 时，必须同步新增同名的 `*GatewayTest.java`。

## TEST-PROJECT-002 文件位置

带有 `@RestController` 的类只允许放在示例项目的 `demo-web` Maven 模块下。
